replica_module = import_da('replica')


class Olympus(process):
    def setup(num_replica:int):
        output(num_replica)

    def run():
        # creating num_replica replicas
        replicas = new(replica_module.Replica, [], num=num_replica)
        start(replicas)


def main():
    olympus = new(Olympus, [ 1 ])
    start(olympus)
